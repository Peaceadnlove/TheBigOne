# ============================================================
# PêchePro — Configuration Render
# Dans Render : ton service > "Environment" > "Add Environment Variable"
# ============================================================

PORT=10000
SESSION_SECRET=pechepro_super_secret_changez_moi_2024

# ── STRIPE ──
STRIPE_SECRET_KEY=sk_test_VOTRE_CLE_ICI

# ── URL de ton app Render (après déploiement) ──
# Nécessaire pour le keep-alive (évite le sleep)
APP_URL=https://TON-APP.onrender.com

# ── EMAIL (optionnel) ──
# BREVO_API_KEY=votre_cle_brevo
# BREVO_FROM_EMAIL=noreply@tonsite.fr
